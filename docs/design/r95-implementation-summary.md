# R95 implementation summary

Implemented on `r95-discovery-density`:

- Existing `banner_key` is selected in the public Projects query.
- `ProjectLanternCard` renders the existing project banner media route when a banner is present.
- Project directory uses compact cards and responsive 3/2/1 layout.
- Members list is reduced to compact scannable rows while Cards view remains available.
- No data migration or CRM changes.
