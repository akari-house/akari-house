# R90 non-destructive scope

R90 deliberately avoids deleting production data. Historical CRM-era House tables are frozen compatibility data pending the R84 evidence gate. This prevents accidental loss of legacy NDA provenance or investor diligence authorization.

The release may remove unreachable code paths and stale UI/copy, but schema destruction requires a later reviewed migration after backups and reconciliation evidence are complete.
