name: Apply Opportunity Workflows

on:
  push:
    branches: [fix/complete-investor-deals-room]

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, 'Apply complete opportunity workflows') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/complete-investor-deals-room
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: node scripts/patch-opportunity-workflows.mjs
      - run: npx prettier --write app/routes/admin-opportunities.tsx app/routes/project-opportunity.tsx
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/routes/admin-opportunities.tsx app/routes/project-opportunity.tsx
          if git diff --cached --quiet; then
            echo "No workflow source changes required."
          else
            git commit -m "Apply complete opportunity workflows"
            git push
          fi
