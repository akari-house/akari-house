name: Footer Horizon Diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  issues: write

jobs:
  footer-diagnostic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium firefox webkit
      - name: Run footer tests and capture output
        run: |
          set +e
          npx playwright test tests/e2e/footer-trust-layer.spec.ts --reporter=list > footer-diagnostic.log 2>&1
          status=$?
          tail -n 260 footer-diagnostic.log > footer-diagnostic-tail.log
          echo "status=$status" >> "$GITHUB_ENV"
      - name: Publish temporary diagnostic issue
        if: env.status != '0'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo 'Temporary browser diagnostic for PR #68.'
            echo
            echo '```text'
            cat footer-diagnostic-tail.log
            echo '```'
          } > footer-diagnostic-issue.md
          gh issue create \
            --repo "$GITHUB_REPOSITORY" \
            --title "Temporary footer horizon diagnostic ${GITHUB_SHA:0:7}" \
            --body-file footer-diagnostic-issue.md
      - name: Enforce focused result
        run: test "$status" = "0"
