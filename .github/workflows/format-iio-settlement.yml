name: Format IIO Settlement

on:
  pull_request:
    paths:
      - ".github/workflows/format-iio-settlement.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx prettier --write app/routes/iio-settlement.tsx app/routes/iio-settlement-export.ts app/routes/health.ts app/routes.ts app/lib/iio-settlement-schema.server.ts tests/unit/iio-settlement-schema.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app tests
          git commit -m "Format IIO settlement release"
          git push