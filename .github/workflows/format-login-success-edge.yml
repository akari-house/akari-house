name: Format Login Success Edge

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Format login success repair
        run: |
          npx prettier --write \
            app/lib/public-login-fallback.server.ts \
            app/lib/public-login-handler.server.ts \
            worker/index.ts \
            tests/public-login-fallback.test.ts
      - run: npm run format:check
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            app/lib/public-login-fallback.server.ts \
            app/lib/public-login-handler.server.ts \
            worker/index.ts \
            tests/public-login-fallback.test.ts
          if git diff --cached --quiet; then
            echo "No formatting changes required."
          else
            git commit -m "Format login success edge repair"
            git push
          fi
