name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply-profile-score-navigation-fix:
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/profile-score-navigation-fix'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/profile-score-navigation-fix
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: python scripts/remove-legacy-xscore-cap.py
      - run: npx prettier --write app/routes/dashboard.tsx
      - name: Commit focused correction
        run: |
          git config user.name "AKARI automation"
          git config user.email "actions@users.noreply.github.com"
          git add app/routes/dashboard.tsx
          if git diff --cached --quiet; then
            echo "The legacy XScore cap is already removed."
          else
            git commit -m "Remove legacy XScore cap"
            git push origin HEAD:agent/profile-score-navigation-fix
          fi

  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      - run: npm run build
      - run: npx playwright install --with-deps chromium firefox webkit
      - run: npm run test:e2e
